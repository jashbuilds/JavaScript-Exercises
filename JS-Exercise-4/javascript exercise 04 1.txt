1. take 3 input fields 'id' , 'name' & salary .

  store this in object as {id:'1' , 'name':'test1' , salary:20000};
  push 3 or more than 3 objects in array.

	a. display array.
	b. display array which contains only name.
	c. display total of salary.
	d. display array which contains object whose name is 'test1'.

2. 

str = 'https://test/google.com';

find the last index of '/' 

3.

let chars = ['A', 'B', 'A', 'C', 'B'];

remove duplicate value from array.

4.
const fruits = ["Banana", "Orange", "Apple", "Mango"];

remove first element from array.


5. 
return array which contains binary value of all elements for the given array. 
 
arr = [1,2,4,5,6] 


6.

let text = "Mr. Blue has a blue house"

- display all this word in array.

o/p : ['Mr.' , 'Blue' , 'has' , 'a' , 'blue' , 'house'] 
	
7.
take a one input field , if in input field entered last character is 'a,e,i,o,u' then background color of input will be red , else green.

8.
take a string as input field:
	- must be 8 character long.
	- must contain one uppercase and one lowercase letter.
	- must contain one symbol.

if following condition is not matched then display message like , "string is not valid".